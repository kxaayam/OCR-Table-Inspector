# Python dependencies for the OCR Table Correction Pipeline.
#
# There are no third-party dependencies: page images are fetched from the
# U of T Library over HTTP using the standard library (urllib), and everything
# else is pure Python. You only need Python 3 and an internet connection.
#
#   pip install -r requirements.txt   # installs nothing; kept for convention
